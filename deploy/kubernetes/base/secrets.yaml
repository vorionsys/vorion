apiVersion: v1
kind: Secret
metadata:
  name: agentanchor-secrets
  namespace: vorion
  labels:
    app.kubernetes.io/name: agentanchor
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # These are placeholder values - replace with actual secrets in overlays
  # or use external secrets management (e.g., External Secrets Operator, Vault)
  database-url: "postgres://user:password@postgres:5432/vorion"
  redis-url: "redis://redis:6379"
  nextauth-secret: "change-me-in-production"
---
# External Secrets example (uncomment if using External Secrets Operator)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: agentanchor-secrets
#   namespace: vorion
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: aws-secrets-manager
#   target:
#     name: agentanchor-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: database-url
#       remoteRef:
#         key: vorion/agentanchor/database-url
#     - secretKey: redis-url
#       remoteRef:
#         key: vorion/agentanchor/redis-url
#     - secretKey: nextauth-secret
#       remoteRef:
#         key: vorion/agentanchor/nextauth-secret
