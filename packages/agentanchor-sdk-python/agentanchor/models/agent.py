# coding: utf-8

"""
    AgentAnchor API

    AI Governance Operating System API - Manage agents, trust scores, marketplace, and governance with complete audit trails.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self

class Agent(BaseModel):
    """
    Agent
    """ # noqa: E501
    id: Optional[UUID] = Field(default=None, description="Unique agent identifier")
    name: Optional[StrictStr] = Field(default=None, description="Agent display name")
    description: Optional[StrictStr] = Field(default=None, description="Agent description")
    status: Optional[StrictStr] = Field(default=None, description="Current agent status")
    trust_score: Optional[Annotated[int, Field(le=1000, strict=True, ge=0)]] = Field(default=None, description="Current trust score (0-1000)")
    trust_tier: Optional[StrictStr] = Field(default=None, description="Trust tier based on score")
    certification_level: Optional[StrictStr] = Field(default=None, description="Council certification level")
    published: Optional[StrictBool] = Field(default=None, description="Whether agent is listed on marketplace")
    capabilities: Optional[List[StrictStr]] = Field(default=None, description="List of agent capabilities")
    created_at: Optional[datetime] = Field(default=None, description="Creation timestamp")
    __properties: ClassVar[List[str]] = ["id", "name", "description", "status", "trust_score", "trust_tier", "certification_level", "published", "capabilities", "created_at"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['draft', 'training', 'active', 'suspended', 'archived']):
            raise ValueError("must be one of enum values ('draft', 'training', 'active', 'suspended', 'archived')")
        return value

    @field_validator('trust_tier')
    def trust_tier_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['untrusted', 'novice', 'proven', 'trusted', 'elite', 'legendary']):
            raise ValueError("must be one of enum values ('untrusted', 'novice', 'proven', 'trusted', 'elite', 'legendary')")
        return value

    @field_validator('certification_level')
    def certification_level_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['none', 'basic', 'standard', 'advanced', 'expert']):
            raise ValueError("must be one of enum values ('none', 'basic', 'standard', 'advanced', 'expert')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Agent from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Agent from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "name": obj.get("name"),
            "description": obj.get("description"),
            "status": obj.get("status"),
            "trust_score": obj.get("trust_score"),
            "trust_tier": obj.get("trust_tier"),
            "certification_level": obj.get("certification_level"),
            "published": obj.get("published"),
            "capabilities": obj.get("capabilities"),
            "created_at": obj.get("created_at")
        })
        return _obj


