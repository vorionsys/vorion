# Orchestrator TODO - 2025-12-01

## Priority Actions

### 1. Cloudflare DNS - Add App Subdomain
```
Cloudflare Dashboard → agentanchorai.com → DNS
Add Record:
  Type: CNAME
  Name: app
  Target: cname.vercel-dns.com
  Proxy: OFF (DNS only - gray cloud)
```

### 2. Cloudflare Pages - Deploy Marketing Site
```
Cloudflare Dashboard → Pages → Create a project
→ Connect to Git → GitHub → chunkstar/agentanchor-website
→ Branch: master
→ Build settings: None (static HTML)
→ Deploy
```

### 3. Cloudflare DNS - Point Root to Pages
```
After Pages deploy, Cloudflare will prompt for custom domain
→ Add agentanchorai.com
→ It will auto-configure DNS
```

### 4. Cleanup
```
GitHub → BanquetAI/agentanchor-website → Settings → Delete repository
```

### 5. Git Branch Decision
**Current State:**
- `main` (remote) = Landing page only (3 files)
- `app-main` = Full application (deployed to Vercel)

**Options:**
- **A) Force push app-main to main** - Replaces landing with app
- **B) Keep separate** - main=landing, app-main=app (update Vercel branch)

---

## Domain Architecture (Target)

```
agentanchorai.com       → Cloudflare Pages (marketing site)
app.agentanchorai.com   → Vercel (application)
```

## Repos

| Repo | Purpose | Status |
|------|---------|--------|
| `chunkstar/agentanchorai` | App (branch: app-main) | ✅ Pushed |
| `chunkstar/agentanchor-website` | Marketing site | ✅ Ready for Cloudflare |
| `BanquetAI/agentanchor-website` | DUPLICATE - delete | ⚠️ Cleanup |

---

Created: 2025-12-01
