# Vorion API Backend Configuration

# Server
PORT=4000
HOST=0.0.0.0

# Security - Change this in production!
API_KEY=vorion-dev-key

# Path to monorepo root (for executing agent commands)
ROOT_DIR=../../

# Logging
NODE_ENV=development
